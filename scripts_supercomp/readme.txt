The scripts in this folder are for use in parameter exploration on a supercomputer.
They are a copy of the code in the /scripts/ folder but variables are set to be
used on a supercomputer instead of local machine.
They rely on some data that is stored in the /scripts/ folder.
